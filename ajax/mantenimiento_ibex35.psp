<%#-*- coding: UTF-8 -*-
registros=int(os.popen("xulpymoney_actualizar_datos_ibex35| grep registros").read().split(" ")[6])

if registros==0:
    req.write ("Ha habido un error en la obtención de datos")
else:
    req.write ("Se han introducido " + str(registros) + " registros.")
%>
