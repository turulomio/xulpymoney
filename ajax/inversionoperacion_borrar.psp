<%#-*- coding: UTF-8 -*-
from core import *
con=Conection().open()
if InversionOperacion().borrar(form['id_operinversiones'])==True:
    req.write ("Se ha borrado el dato")
else:
    req.write ("Ha habido un error en el borrado del dato")
con.Close()
%>
